---
tags:
  - 酒馆助手
  - SillyTavern
  - 前端开发
  - 脚本开发
  - 开发环境
date: 2026-05-26
source: StageDog/tavern_helper_template 教程
---

# 酒馆助手前端与脚本开发指南

> 基于 [tavern_helper_template](https://github.com/StageDog/tavern_helper_template) 编写模板的开发方法论。
> 酒馆助手文档：<https://n0vi028.github.io/JS-Slash-Runner-Doc/>

---

## 一、核心概念：脚本 vs 前端界面 vs 流式楼层界面

### 1.1 脚本（Script）

在后台运行，受开关控制。

- **启用时**：执行一段初始化代码
- **运行期间**：监听事件（酒馆事件、按钮点击、脚本卸载）并响应
- **能力范围**：修改酒馆页面 DOM、监听/修改提示词、添加设置页、控制世界书……

> 示例：MVU 变量框架脚本——监听"收到新消息楼层"事件，解析 `<UpdateVariable>` 命令更新变量。

### 1.2 前端界面（Frontend UI）

在前台消息楼层中显示，与消息楼层绑定。

- **原理**：酒馆正则捕获 AI 回复文本 → 替换为含 `<body>` 的 HTML 代码块 → 酒馆助手渲染为界面
- **加载时**：执行一段初始化代码
- **运行期间**：监听事件、与玩家交互
- **局限**：必须在 AI 回复完成后才能渲染，流式传输期间只能看到代码块

### 1.3 流式楼层界面（Streaming Floor UI）

**用脚本实现的前端界面替代方案**——不需要等 AI 回复完成即可渲染。

- **原理**：脚本用 jQuery 隐藏酒馆原生楼层，自行监听流式事件渲染界面
- **关键事件**：
  - `tavern_events.MESSAGE_SEND` → 玩家触发 AI 生成
  - `tavern_events.STREAM_TOKEN_RECEIVED` → 获取流式文本
  - `tavern_events.MESSAGE_RECEIVED` → AI 回复结束
- **框架支持**：模板 `util/streaming.ts` 提供了流式楼层界面框架

```
流式楼层界面 = 脚本的能力 + 前端界面的显示效果 + 流式传输支持
```

---

## 二、开发工作流

### 2.1 完整流程

```
src/ 中新建项目 → AI 编写代码 → pnpm build 打包 → 复制到酒馆
```

1. 从 `初始模板/` 对应文件夹（脚本/前端界面）复制到 `src/` 并重命名
2. 发给 AI 编写需求（**让 AI 一点点做，做一部分测试一部分**）
3. `pnpm build` 打包
4. 前端界面：`dist/` 中的 `.html` 用代码块包裹后贴到酒馆正则的「替换为」
5. 脚本：`dist/` 中的 `.js` 复制到酒馆助手脚本库的「脚本内容」

### 2.2 实时开发工作流（核心）

避免每次修改都要手动 build + 复制粘贴。

#### Step 1：自动打包

```bash
pnpm watch    # 替代 pnpm build，监听文件变化自动重打包
```

#### Step 2：酒馆连接打包监听器

酒馆助手 → 开发 → 实时监听 → 启用「允许监听」。WiFi 图标变绿即连接成功。

#### Step 3：打包结果映射为网络链接

Cursor 右下角点击 **Go Live**（Live Server 扩展），将文件夹映射为 `http://localhost:5500`。

#### Step 4：酒馆从链接加载

前端界面正则替换为改为：

```html
<body>
  <script>
    $('body').load('http://localhost:5500/dist/项目名/index.html')
  </script>
</body>
```

脚本改为一行：

```js
import 'http://localhost:5500/dist/项目名/index.js'
```

> 模板 `初始模板/` 中已提供实时修改正则和脚本模板，只需替换其中的链接。

---

## 三、让 AI 直接看/操控酒馆页面

### 3.1 Chrome DevTools MCP（推荐）

1. Chrome 输入 `chrome://inspect/#remote-debugging`，勾选「Allow remote debugging」
2. **完全关闭浏览器**或重启电脑
3. 模板已为多数编程助手配置好 MCP

AI 能做的事：
- 读取页面显示、Console 日志、网络请求
- 读取你通过开发者工具选中的元素
- **像素级复刻网页样式**
- **自己点击网页、填写表单、甚至像真人一样玩酒馆**

> 提示词中强调：`用 chrome-devtools-mcp 来访问酒馆网页`

### 3.2 Browser MCP（备选）

功能弱于 Chrome DevTools MCP，只能看不能操控。

1. 安装浏览器扩展：[Browser MCP](https://chromewebstore.google.com/detail/browser-mcp-automate-your/bjfgambnhccakkhmkepdoekmckoijdlc)
2. 在酒馆页面上点击扩展图标 → 连接

---

## 四、AI 编程助手配置速查

模板已为以下助手预配置了规则和 MCP：

| 助手 | 规则文件 | MCP 配置 | 备注 |
|------|---------|---------|------|
| **Cursor** | `.cursor/rules` | `.cursor/mcp.json` | 自动读取 |
| **Claude Code** | `CLAUDE.md` | `.mcp.json` | 推荐，自动读取 |
| **Codex** | `AGENTS.md` | 需手动配置 | 据说目前最好用 |
| **Kilo Code** | `.kilocode/rules` | `.kilocode/mcp.json` | 推荐，自动读取 |
| **RooCode** | `.roo/rules` | `.roo/mcp.json` | 自动读取 |
| **Cline** | `.clinerules` | 按 `.cursor/mcp.json` 自行配置 | 自动读取规则 |
| **Gemini CLI** | `GEMINI.md` | 按 `.cursor/mcp.json` 配置到 `.gemini/settings.json` | 自动读取规则 |
| **Augment** | `.augment/rules` | 按 `.cursor/mcp.json` 自行配置 | 自动读取规则 |

> ⚠️ AI 可能偷懒不读规则。提示词中强调：**「阅读现有规则」** + **「通过 MCP 访问酒馆网页」**

---

## 五、进阶技巧

### 5.1 前端界面正则最佳实践

**原则：正则只负责定位，数据处理交给代码。**

```yaml
# 正确：宽松匹配，只定位
查找正则表达式: <status>.*</status>

# 错误：在正则里处理数据
查找正则表达式: <status>\n心情:→\((.*)\)\n衣着:(.*)\n...
```

在代码中用 `getChatMessages` 获取整条消息再解析。

### 5.2 避免渲染卡顿

代码块越大，酒馆渲染越卡。改为链接加载：

```html
<body>
  <script>$('body').load('https://testingcf.jsdelivr.net/gh/...')</script>
</body>
```

### 5.3 发布自动更新的界面/脚本

利用 jsdelivr CDN（国内用 `testingcf.jsdelivr.net` 镜像）：

```
https://testingcf.jsdelivr.net/gh/组织名/仓库名/路径
```

模板支持 GitHub Actions 自动打包 + 版本号管理，12h 刷新 CDN 缓存。

手动刷新：<https://www.jsdelivr.com/tools/purge>（主服务器立即刷新，镜像不刷新）。

### 5.4 用 Figma 像做 PPT 一样设计界面

1. Figma 有大量预制组件（含大厂原生设计）
2. 设计完成后，配置 Figma MCP 让 AI 直接按设计稿写代码
3. Figma 自身也有 AI 功能，生成的界面一般比通用 AI 好看

### 5.5 流式界面实现要点

**同层前端界面方案**（简单）：
- 只在一个前端界面内游玩
- 用 `generate`/`generateRaw` 自行触发 AI 回复
- 监听 `iframe_events.STREAM_TOKEN_RECEIVED_FULLY` 获取流式文本
- 用 `setChatMessages` 等 + `{ refresh: 'none' }` 静默存档

**流式楼层界面方案**（灵活）：
- 用 jQuery 隐藏原生楼层
- 监听 `STREAM_TOKEN_RECEIVED` 自行渲染
- 模板 `util/streaming.ts` 已提供框架，AI 直接会用

### 5.6 脚本中修改提示词

```ts
// 按事件发生顺序，可选的事件钩子：
// GENERATE_AFTER_COMBINE_PROMPTS
// CHAT_COMPLETION_PROMPT_READY        ← 常用
// GENERATE_AFTER_DATA                 ← 提示词模板/宏发生在这里
// CHAT_COMPLETION_SETTINGS_READY

eventOn(tavern_events.CHAT_COMPLETION_PROMPT_READY, (event_data) => {
  // 用 splice/push 等原地修改，不要重新赋值 event_data.messages
  _.remove(event_data.messages, (m) => m.role === 'user');
});
```

### 5.7 Vue 技术栈

模板直接支持 Vue 3 + 配套库：

| 库 | 用途 |
|------|------|
| `vue` | 响应式 UI 框架 |
| `pinia` | 状态管理 |
| `vue-router` | 路由 |
| `vueuse` | 组合式工具库（缓存、全屏、拖拽、通知……） |
| `gsap` | 动画（几行代码实现打字机效果） |

安装 [Vue DevTools](https://chromewebstore.google.com/detail/vuejs-devtools/nhdogjmejiglipccpnnnanhbledajbpd) 浏览器扩展获得调试面板。

### 5.8 其他技巧

- **代码混淆**：在 `index.ts` 加一行 `// @obfuscate`
- **代码分块**：删除 `webpack.config.ts` 中的 `LimitChunkCountPlugin`
- **导入文件文本**：`import string from './file?raw'`
- **与外部通信**：模板支持 `socket.io-client` 等所有浏览器端库
- **断点调试**：设断点后 `F5` 启动调试，浏览器运行到断点处自动暂停
- **免费资源**：
  - 字体：[ZeoSeven Fonts](https://fonts.zeoseven.com/)
  - 图标：[FontAwesome](https://fontawesome.com/)
  - CDN：[jsdelivr](https://www.jsdelivr.com/)（国内用 `testingcf.jsdelivr.net`）
  - 颜色对比度：[Adobe 颜色对比度检查器](https://color.adobe.com/zh/create/color-contrast-analyzer)

---

## 六、一句话总结

```
scripts/    → 后台运行，改 DOM、改提示词、做流式界面
frontend/   → 前台显示，AI 回复完成后渲染，正则只定位不处理数据
streaming/  → 脚本伪装成前端界面，流式传输期间就能渲染交互

开发时：pnpm watch + Go Live + MCP 让 AI 实时看到页面
发布时：pnpm build → jsdelivr CDN 永久链接
```
